{
  "mcpServers": {
    "attending": {
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "attending.mcp_server"],
      "env": {"PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/../../src"}
    }
  }
}
